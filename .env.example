# API 監聽設定
# 127.0.0.1 = 僅限本機；0.0.0.0 = 開放同一區網裝置連線
ENTRUST_API_HOST=127.0.0.1
ENTRUST_API_PORT=8000

# 使用 0.0.0.0 時必填。可用 `openssl rand -hex 32` 產生。
ENTRUST_API_TOKEN=請換成長且隨機的字串

# Docker/noVNC 部署使用。傳統 VNC 驗證只使用前 8 個字元。
ENTRUST_VNC_PASSWORD=請換成8位英數密碼
